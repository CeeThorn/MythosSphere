import React from 'react'
import ReactDOM from "react-dom"
//

ReactDOM.render()

//what i want to render, where do i want to render it
//needs to point to div with the id of root document.getEleementBYId("root") 

//JSX is a syntax extension for JavaScript that allows you to write HTML-like code within JavaScript files. It is commonly used with React to describe what the UI should look like. JSX gets transpiled into regular JavaScript by tools like Babel before being executed in the browser.